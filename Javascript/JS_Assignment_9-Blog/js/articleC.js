var main = document.getElementById('main')
class Article {
    constructor(){
        this.article = create('article',main)
    }


    function create(element, container) {
        var article = document.createElement(element)
        // container.appen
    }
}